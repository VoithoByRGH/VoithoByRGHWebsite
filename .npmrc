# Allow CRA/webpack to find dependencies in a flat node_modules
shamefully-hoist=true

# Automatically install missing peer dependencies
auto-install-peers=true

# Ensure peer dep conflicts don’t block install (npm-style flag for compatibility)
legacy-peer-deps=true

# Don’t enforce engines (since CodeSandbox might not match exactly)
engine-strict=false
